# Database

This folder stores the SQLite database used for conversation memory.

The runtime database (`memory.db`) is excluded from GitHub using `.gitignore`.

Running the application will automatically create a new `memory.db` file.